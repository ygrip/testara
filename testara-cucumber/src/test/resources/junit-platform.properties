# These tests drive TestaraObjectFactory/JUnit4ScopeContext static state directly, mirroring the
# same static state a real JUnit4 Cucumber run would use - keep execution sequential to avoid
# cross-test interference.
junit.jupiter.execution.parallel.enabled=false
