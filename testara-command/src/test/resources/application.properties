author=yunaz.gilang
property.name=${HOME:}-of-${USER:}
property.command=${properties(author):}-of-${timestamp():}
property.command.fallback=${SOME_KEY:properties(author)}-of-${SOME_OTHER_KEY:uppercase(default)}
property.fallback=${SOME_KEY:default}-of-${SOME_OTHER_KEY:default}
internal.game.clients=quest,referral